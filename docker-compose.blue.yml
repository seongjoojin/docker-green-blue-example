
version: '3.7'

services:
    nextjs:
        build:
            context: .
            dockerfile: Dockerfile.nextjs
        image: nextjs_blue:0.1
        ports: 
            - "3001:3000"
        container_name: nextjs_blue
        deploy:
            resources:
                limits:
                    cpus: '0.50'
                    memory: 50M
                reservations:
                    cpus: '0.25'
                    memory: 20M
